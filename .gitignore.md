场景 1：只忽略根目录下的 .vscode 文件夹，并保留所有 important.log
        gitignore
        /.vscode/
        *.log
        !important.log
    /.vscode/ 加了斜杠开头，只忽略根目录下的 .vscode 文件夹，不会影响其他路径。

    !important.log 会取消忽略所有位置的 important.log（包括根目录和子文件夹）。

场景 2：完全忽略 .vscode/ 内部所有内容，但全局保留 important.log
    如果 .vscode/ 里真的有你需要的 important.log，你不能忽略整个文件夹，需要改成：

        gitignore
        .vscode/*
        !.vscode/important.log
        *.log
        !important.log
    先忽略 .vscode/ 下的所有内容（*），再单独取消忽略 important.log。

    同时全局 *.log 也被取反规则 !important.log 覆盖。

场景 3（最常见）：忽略所有 .log，只保留根目录的 important.log
        gitignore
        .vscode/
        *.log
        !/important.log
    加了 / 开头，明确只取反根目录下的 important.log，子文件夹里的 important.log 依然被忽略。